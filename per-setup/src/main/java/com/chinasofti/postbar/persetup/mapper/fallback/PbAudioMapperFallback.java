package com.chinasofti.postbar.persetup.mapper.fallback;

import com.chinasofti.postbar.persetup.dto.AudioDto;
import com.chinasofti.postbar.persetup.mapper.PbAudioMapper;

public class PbAudioMapperFallback implements PbAudioMapper {
    @Override
    public void insertAudio(AudioDto audioDto) {

    }
}
